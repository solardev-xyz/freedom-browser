# Copy this file to .env.agent-tests.local and fill only the credentials
# required for the live agent tests you intend to run.
FREEDOM_FREE_PI_TEST_API_KEY=

# Opt in to the browser-tool qualification separately from the text smoke.
# The current pilot model is not yet qualified because it ignores tool calls.
FREEDOM_FREE_PI_AGENT_EVAL=0
