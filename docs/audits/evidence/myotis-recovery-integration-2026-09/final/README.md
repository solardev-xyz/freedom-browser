# Final source qualification with ownership guard

These fresh runs repeat the real Electron main/manager/process/supervisor/addon/Colibri-worker recovery qualification after the durable ownership guard was completed. Prior captures remain unchanged in the parent directory. Run time: 2026-09-14 20:32–20:33 UTC, macOS arm64, Electron 44.3.0 / Node 24.20.0.

Both chains passed. Ethereum took 79.9 seconds; Gnosis took 6.7 seconds. Ethereum had four ordinary bounded caller timeouts (two before each successful proof read); Gnosis had none. Each chain detected the authentic historic stale anchor, verified a live recent checkpoint through the actual production worker, retired its old child, created a new authenticated generation, reached SYNCED/readiness, returned a verified public zero-address account proof, stopped, restarted the same authenticated generation, returned another verified proof, and stopped cleanly. Successful account proofs have `peerProofValid`, `blsVerified`, and `beaconChainVerified` true and `failReason` null.

Before each live run, the actual manager also tested two isolated legacy ownership records: active and unknown. All four controls returned startup=false, readiness=false, and the recoverable UI state `recovery-blocked` with reason `ownership`. Owner bytes remained unchanged; each directory contained only its original ownership record, with no generation created. No native start-attempt appeared until the later actual sync fixture. Stop succeeded in every case.

The stale fixture uses authentic upstream v0.1.7 August 9 root/slot pairs with explicitly synthetic historical persisted verification metadata. Actual recovered checkpoint metadata is from live verification. No snapshot existed on either restart, so these are authenticated checkpoint reboots, not persisted sync-committee snapshot restoration. The same limitations in the parent README apply: no claim about long-duration sync, other platforms, renderer interaction, or package signing. No private profile or credentials are included; all temporary native state is retained outside this evidence directory.

`summary.json` records final source hashes, checks both proof outcomes and ownership controls, and identifies the exact addon. Source hashes were also checked while the Ethereum process was running and matched the final capture. `*-stdout.log` preserve all supervisor lifecycle receipts and transient read failures. All three native child generations for each chain exited with verified reaped receipts, both Electron campaigns exited0, and an OS process check confirmed no campaign child/supervisor remained.

To reproduce, follow the parent README but use this directory's `electron-main.js`; use a fresh temporary directory and chain ID1 or100, with600-second total budget. The only additional setup performed by this harness is the isolated active/unknown ownership fixture. It does not edit production code or load a user's profile.
