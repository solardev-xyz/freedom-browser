# Packaged workspace qualification — 2026-09-08

Status: qualification in progress. Current packaged macOS idle/running native Quit, browser-main loss and supervisor-loss limitation assertions have passed. The bounded detached case and current packaged Linux campaign remain incomplete. The user selected packaged-app qualification, including bounded detached cases, and explicitly added Linux regression coverage. macOS runs belong on the disposable Mac; Linux runs use its existing disposable non-root identity. The primary Mac runs ordinary unit tests and source review only. No new third-party software, credentials, OS configuration, release publication or signed/notarized qualification is implied.

The existing platform qualification configurations replace the package's main entry point with `electron-qualification-main.js`. Those artifacts establish backend/asar evidence, but do not by themselves exercise the real browser's shutdown. The new qualification needs `src/main/index.js` in `app.asar`, actual packaged application identity, and platform-specific runtime/cleanup observation. Optional node payload omissions and any credential-free test substitutions must be reported explicitly.

## Bounded detached test support

The existing test-only `prepareAgentExitScenario` detached mode previously had unbounded heartbeat loops. Both parent and forked child now arm their own expiry alarms, with the child doing so before session changes or I/O. Each records shared `CLOCK_MONOTONIC` and wall-clock alarm anchors plus PID, parent, session and process-group identity. These are fixture safeguards and observations, not product termination guarantees. The fixture remains one parent and one child; the configured expiry is limited to 1–15 seconds.

Detached mode now reserves its token before workspace preparation, execs the fixed Python entry point, and retains raw executor, mapped terminal and native supervisor evidence through the existing observational wrapper. An exception after child creation leaves execution observation in place until the actual result settles. These changes stay in main's existing TEST_MODE harness; no renderer, IPC, sandbox policy or production execution authority changes. Pure mocks cover timer ordering, token replay, receipt preservation and partial-setup failure without executing Python or native processes.

Validation: focused 16 tests passed; ordinary full Jest passed 4,119 tests across 238 suites, with 54 tests / seven suites skipped, and lint passed. Logs on the primary Mac: `/private/tmp/freedom-packaged-detached-{focused,tests,lint}.log`. No actual detached/app-exit fixture ran there.

## Linux source-review finding

The Linux inventory reports installed Bubblewrap 0.9.0 and an existing dedicated `freedomqual` identity (uid 1001). Its newest relevant checkout is `fcd05dfcbed4a325a16003ca0686f12d1419a28d`, which does not contain the current source; no old run is assigned to the new candidate. Current dependency/asset compatibility and source transfer remain preparation gates.

Independent Claude request `local/req-aada700aa376b340471e41f297531238` identified a pre-existing cancellation race in `bubblewrap-backend.js`: it stores the reported namespace-init PID and signals that number while checking only the outer Bubblewrap child's JavaScript exit state. The outer Bubblewrap can reap the init before Node receives the outer process's exit callback, leaving a window in which that number could be reused. This is source-derived, not an observed PID-reuse exploit or a regression introduced by macOS supervision.

The main agent checked the matching [Bubblewrap 0.9.0 monitor and parent-death implementation](https://github.com/containers/bubblewrap/blob/v0.9.0/bubblewrap.c#L438) and [documented `--die-with-parent` behavior](https://github.com/containers/bubblewrap/blob/v0.9.0/bwrap.xml#L481). Removing the direct init signal may avoid that class of stale target, but must be reviewed against initialization races and verified namespace teardown before adoption. JavaScript ChildProcess signaling has a separate, narrower ownership concern from the retained Node/libuv review; it is not promoted to a kernel-bound handle by this finding. Namespace isolation and signal-target identity are distinct properties.

No Linux backend change or runtime qualification follows from this source finding alone. The concrete plan must retain it, establish safe test-only fault authority, and use the disposable UID. No PID-reuse stress, host-wide PID cleanup or arbitrary process-name signals are authorized.
The follow-up Linux report `hetzner1/req-ded995b1328ce5536d05304b0905ebcf` rejects direct-init-signal removal as an all-state fix: initial setup can precede parent-death-handler installation. The backend therefore remains frozen for this functional qualification. A separate owned test PID namespace is required as a backstop around the browser/display/fixtures, in addition to the dedicated UID and pidfd-based harness authority. The outer namespace must stay alive during observation; its cleanup is never product teardown evidence. The uid-1001 baseline was empty at the reported snapshot, which is not an exclusive reservation and must be rechecked before launch. Package-lock and assets were verified unchanged from the donor. No Linux execution occurred during these reviews.

The first Linux driver draft (`hetzner1/req-644f4fffe3f93e221f8fac8c24ec7d2e`) passed 37 Python and five Node pure checks, with clean lint. It supplies a dedicated-user namespace envelope and an idle `app.quit()` bridge; running/crash/composition cases explicitly refuse execution until their current-source bindings exist. The main agent received and verified all seven members of its source archive (37,757 bytes, SHA-256 `b71aaa1fd952da55ddeaf7a3e5bad44ed0d6b676fa286b80e5e1ec774f81d42d`). Review found that idle success must require the browser's actual zero exit status, and that outer exception cleanup needs bounded exit/reap confirmation. Inspector context setup also needs explicit validation. These are draft-harness corrections, not observed product failures; no Linux namespace or application run followed from the mock passes.

The revised source-only draft (`hetzner1/req-3df717feb1e68edde3a374a980413644`) retains direct-child terminal and reap statuses, requires normal browser exit zero before proving idle Quit, and divides the same 30-second budget into 26 seconds for proof, two for inner cleanup and two for outer confirmation. The bridge explicitly selects and checks the default Node/Electron inspector context. All seven revised source members were hash-verified from the 45,389-byte archive, SHA-256 `6c5cdc54e265f01bd6ebafb0eecd1f0966f26439520dfce5e908aa8f392ab7ef`. Remote pure tests passed 56 Python and 13 Node checks. Remote lint could not access its installed donor tools; the main agent linted both revised JavaScript files through the existing local ESLint configuration and they passed. Kernel behavior, current-source fixture bindings and actual packaged Linux runs remain unqualified.

## First macOS packaging attempt

Disposable-Mac request `macmini/req-63ca78fb5cf3cab0e31556b9c3071446` stopped at its first build failure. The installed electron-builder CLI interpreted its own script path as an argument under Electron-as-Node and exited with status 1 before creating package output. No packaged browser or detached fixture ran. The original build root had a matching exit event and was reaped; the report records no emergency signals or unconfirmed registered identities. This is a build-launch failure, not a product lifecycle failure or a passing packaged qualification.

The candidate's verified source subtree was `dcec589b912f178599d47f1c5bd9e17434977704` (test-support commit `252f4657`); the native helper and manifest were unchanged. Raw evidence remains under `/private/tmp/freedom-packaged-run-xx3r_6gu` on the disposable Mac. The main agent verified the returned source-only bundle (83,222 bytes, SHA-256 `2d612f60c1a3c3fb5cfaa216e8d33065a7dab089aea23b1aa388d036537cd2b8`) and reviewed the proposed fixed programmatic `electron-builder.build` invocation. A fresh, separately recorded attempt is in progress; the failed attempt is retained.

Two subsequent build attempts also stopped before any packaged browser launch:

- `macmini/req-ef496ffb72a85820b75cbff3fbc100a6`, root `/private/tmp/freedom-packaged-run-3p2rtp25`: the programmatic build reached distribution copying, but Electron's patched filesystem interpreted `default_app.asar` as a virtual directory. The archive was present and its raw hash was retained. The build root and observed npm child both had original exit-status-1 events; the root was reaped after signal authority was retired. No emergency signals or unconfirmed registered identities were reported. Partial output is not a completed package. The returned source bundle was verified: 94,598 bytes, SHA-256 `3c8ee761f99b60b7dc13add50605b6487207402defc62f5a04a76d3249860863`.
- `macmini/req-e61c65698e70f38a9bae02fe33a8f62a`, root `/private/tmp/freedom-packaged-node-2re7r_zp`: installed plain Node 22.22.0 avoided archive virtualization. The monitor then rejected npm during its `/usr/bin/env` launch stage, before registering that child's exit event. The direct builder was killed and reaped; one earlier npm child had a known exit, but the original outcome of observed child PID 81594 is **unknown**. A subsequent read-only absence check does not recover that evidence. The attempt is failed, with emergency cleanup and incomplete observed-process accounting. Its 258-file partial output is unqualified. The returned source bundle was verified: 73,706 bytes, SHA-256 `4768a444c7cfdd10bacd7b970d9192b58d17fe6fdd3103ceb3ff3c497391cf89`.

The next monitor correction must register original exit events before launch-image inspection and distinguish the fixed npm launcher transition from signal authority. All three attempts retain the same current product source and helper hashes; none is a product lifecycle pass or a completed packaged artifact.

The corrected monitor's first run (`macmini/req-ca9e1b128df2bb36e5c81d252058cc8e`, `/private/tmp/freedom-packaged-monitor-x3n1d8ds`) still failed because npm changed its visible process title. This time the child had an original exit registration and its status was retained; the direct builder required emergency cleanup. No package output or application run resulted. Independent review and the 42,511-byte source bundle were retained and verified, SHA-256 `31275a889fa55aab0833862cbc887102b641d57a660132592d22620500b49b19`.

## macOS build completed; dependency preflight failed

Request `macmini/req-86c7d45fda19da2acb375be87326c9f2` built an unsigned package in 13.396146 seconds under `/private/tmp/freedom-packaged-run-sticky-lz3cf2pc`. The monitor now keeps an originally registered, pinned Node instance as observation-only when argv changes; that state cannot regain signal authority. All four observed build instances had original exit events, with no emergency cleanup or unconfirmed statuses. The builder and icon tool exited zero; both npm inventory commands exited one, which is retained alongside the builder's successful result. The root's signal authority ended before its sole reap. Historical PID 81594 remains unknown.

The new `app.asar` is 160,399,638 bytes, SHA-256 `ddbcc613ec1ed7efd118dd172b5a164cebe06d006d74e7a25ae4618f1155be2b`. Inspection compared 374 shipped source files with the candidate (matching the local count of tracked source files after test exclusions), 14 unpacked native addons with their installed donors, and retained 14 framework executable hashes. The helper/manifest hashes were unchanged. These are artifact inspection results; the controller's expected `isPackaged` field is not an observed browser-main identity.

The combined packaged-runtime preflight then failed with `Cannot find module 'bindings'` when constructing an in-memory SQLite database. Both packaged and installed `better-sqlite3` 12.11.1 declare this dependency; it exists in the installed donor but is absent from the archive. Sequential resolver assertions ran before that failure, but no complete successful preflight record was emitted. No GUI, real app-main lifecycle, or detached case ran. Dependency collection/staging requires correction using existing installed dependencies; a successful build alone does not close qualification. The returned source/artifact-metadata bundle was verified: 65,279 bytes, SHA-256 `b2a136bb1510fba2be98d26563c4c3c01160da10cc24ce4497f92e7ea535e2d4`.

## Corrected macOS dependency closure; first real packaged idle attempt

Request `macmini/req-702d087defd7c4ccc67b66825eefd91c` confirmed the staging cause: the candidate's symlinked `node_modules` changed npm's dependency inventory. The linked inventory exited one with 6,936 problems and produced 251 collector graph nodes; the original donor and a fresh complete materialized copy both exited zero and produced 404 nodes. The correction copied existing installed bytes into fresh staging, verified hashes/modes and retained the original symlink. No dependency, version, native binary, source or original checkout was changed or acquired. The closure check covered 775 declared edges across 408 packages plus the app, with zero missing required dependencies; existing overrides and 19 optional absences are recorded explicitly.

The fresh build under `/private/tmp/freedom-packaged-run-closure-uq8cmxum` completed in 13.598714167 seconds. All four observed build roles had original registered exit statuses; the no-workspace npm-prefix query exited one, while npm-list, the builder and icon tool exited zero. No emergency or unconfirmed build status was reported. The archive is 240,005,152 bytes, SHA-256 `b00bcad04aea640bf131a5a6183a3c7a9db4e19b8d4e3441a47b8b18b9405a5e`. Its 408 package versions, 33 unpacked native addon byte hashes and 374 shipped source files match their retained installed/candidate inputs. The helper and manifest remain unchanged. The actual packaged executable's RunAsNode preflight separately persisted resolver success and then passed an in-memory SQLite query. This verifies packaged resolution and dependency availability; it does not load the real app main or prove GUI lifecycle behavior.

The first actual packaged idle case, `execution-df705d9b/quit-idle-vitrgbmo`, **failed** after 1.847159 seconds. The controller timed out evaluating its isolation-ready marker, and framework enrollment then raised `string indices must be integers`. No preparation, native Quit, command or supervisor was reached. Browser PID 83003 required emergency cleanup; it and bridge PID 83004 have original SIGKILL exit events and sole reap records. Framework PID 83007 was first observed but never registered, so its original outcome is **unconfirmed**. The recorded process counters are incomplete: four roles are known including the watchdog and that first-read framework child, but only browser and bridge have original registrations. Later absence cannot repair this gap. All historical unknown statuses remain unknown. Running, main-loss, supervisor-loss and detached packaged cases were not attempted.

The framework producer wrote hash strings where the consumer expected structured byte/hash records. This needs a producer-to-consumer test and registration before later image validation, without granting signal authority on failed validation. Separately, source inspection shows Electron 43's [`-r` processing belongs to the default development app](https://github.com/electron/electron/blob/v43.0.0/default_app/main.ts#L31); [real packaged startup loads its own main directly](https://github.com/electron/electron/blob/v43.0.0/lib/browser/init.ts#L168). The former preload mechanism cannot establish packaged credential isolation. A fixed debugger pause before application code, using the existing capability-removal shim, is being prepared for review without modifying the artifact or calling real keychain methods. No retry or timeout-only workaround is credited here.

The main agent verified every returned text member and the complete selected source/evidence JSON: 209,665 bytes, SHA-256 `a4d198e60485efba13607e5f48987d727f5b5c47b56f11a1f61203b797dd4939`. Larger dependency inventories and raw artifact records remain in the remote root with retained hashes. This is an unsigned package with explicitly reduced optional payloads; real-keychain, live-provider and signed/notarized release behavior remains unqualified.

## Linux kernel-only preparation and first attempt

The third source draft (`hetzner1/req-2ff1be23e72c88f27d7e45bef15090b8`) preserves readonly remount flags, rejects overlapping mount roots and distinguishes terminal-process observation failures from exit proof. Its seven source members were verified from the 48,080-byte archive, SHA-256 `db119ddbf416242492b3018e5c323492908c78fe3dcf75abf9369f1d01522d53`. Pure tests passed 79 Python and 13 Node checks, and the main agent's existing ESLint passed both JavaScript files. The outer test namespace also contains display/bridge roles when those are eventually launched; it is not an empty product-only ownership boundary.

The single kernel-only preflight request `hetzner1/req-90f7449aa9fbba9a10164885b283d0fa` **stopped before namespace creation**. The fixed uid-1001 Python entry could not import `linux_driver` from its root-owned mode-0711 source directory. Installed Python's module discovery lists directories, so lack of listing permission is the supported setup diagnosis, not a demonstrated kernel incompatibility. There was no namespace init, pidfd acquisition, mount, app/display/Bubblewrap workload or fault. The wrapper exited one. Eight additional mocked checks passed before this failed attempt; they do not establish runtime success.

Default-tool before/after observations came from differing namespace/UID views, so they prove neither host mount invariance nor a host mutation. No namespace-init zero exit or reap record exists. The next source-only correction prepares readable immutable harness staging and one consistent host observer around the fixed non-root launch. No OS policy changes or root workloads are authorized. Current repository-source staging remains blocked by automatic approval review pending the user's specific approval; kernel-only preparation does not bypass that block.

The main agent verified the returned 13-member text archive: 37,424 bytes, SHA-256 `b3f3ca3b517d3e5090cba2dae6e37d2fbe271ed0797273883c15e3cac87e046e`. Original failed evidence is retained under `/tmp/herdr-peer-kernel-preflight.DdnKOW` on Linux. Both platforms still require actual current packaged lifecycle evidence.

## Actual packaged idle native Quit — passed

The reviewed debugger-startup harness (`macmini/req-5517728514bab26dbbe66083a536d5da`) passed 17 JavaScript test entries, 22 Python mocks and lint before execution. The main agent verified all 71 text members of its 426,216-byte source bundle, SHA-256 `6adc964a0a401e0daf12d81abe95d47dbb6dae249fdd3ad0e2057d1158cdb667`. Framework registration now precedes image validation, and the actual producer/consumer records agree. A preservation deviation remains explicit: its copied verifier rewrote the prior root's `source-proof.json` with identical bytes, changing its timestamp. Independent archived proofs retain that same hash; no previous result, log or app bytes changed. The fresh verifier's output path was corrected.

The single authorized idle attempt (`macmini/req-51b846c66a295d367fb26adfbf20382e`) passed in 1.019653 seconds at `/private/tmp/freedom-packaged-run-preentry-_vcgv7y5/execution-sk93fod7/quit-idle-4jhezz6w`. Startup paused at the exact packaged `src/main/index.js` breakpoint (script 137, zero-based line 1, column 24, reason `ambiguous` with the owned breakpoint hit). No bootstrap fallback was used. The frame installer verified actual packaged identity and source, loaded the fixed unavailable-storage shim while `module.loaded` was false, and persisted the matching marker before resuming. Real app readiness, packaged identity, zero-option supervisor resolution and the app-owned idle preparation then passed. The same `b00bcad…` archive and all 55 checked artifact/source records matched before and after.

Native `terminate:` was invoked by browser PID 84134. Its original registered exit status and direct reap were both zero, 280.591 milliseconds after the watchdog's action timestamp, with ordered shutdown logs and no drain warning. All five observed framework helpers had original exit events (four zero, one SIGTERM); none needed watchdog signaling. Eight instances were observed including the watchdog, with no unconfirmed registered role. Controller PID 84135 was explicitly disposed of with SIGKILL after browser proof and reaped; that harness intervention is not a natural controller exit or product cleanup. Idle creates no managed command, so command receipts, heartbeat and expiry are not applicable. Counts describe observed processes, not complete Chromium birth history or OS containment.

The [retained evidence bundle](macos-packaged-native-idle-2026-09-08.bundle.json) is 175,054 bytes, SHA-256 `2eda6135a4635b3380c77a3b2ee62e8eba6a4ef4a3f895728e9b022e483523db`. The main agent verified all 32 text members. It distinguishes raw records from explicitly transformed copies that remove the inspector endpoint or executable approval fields, with original/transformed hashes and paths. Configuration, permits and audit/reply credentials are excluded. Fixed fixture identity tokens are not credentials.

Logging limits remain: the full `getScriptSource` and possible-breakpoint responses were checked at runtime but not persisted; the ACK trace held a mutable identity map that later included two helpers registered after the action. Only timestamped per-role events establish enrollment order. Retirement before direct reap is enforced by the reviewed source and final state, without a separate timestamped retirement event. The prior failed attempt and its unknown framework PID 83007 are not repaired by this new pass. Missing optional artifacts, test stubs and logged startup warnings remain visible. This completes only credential-free, unsigned packaged idle Quit; running, main-loss, supervisor-loss, detached, provider and signed-release gates remain separate.

## Actual packaged running and failure campaign — passed within stated limits

Request `macmini/req-2b549b283bd58c47f788b3f40a68bade` ran exactly three ordered cases under `/private/tmp/freedom-packaged-run-three-axfxqass/execution-cs_52xyl`, without retries, rebuilding or modifying the product. Focused validation covered the fixed case scope, actual action path, immutable trace snapshots and retained debugger responses: 15 JavaScript test entries, nine Python mocks and lint passed. All 55 checked artifact records and source subtrees matched before and after, including the unchanged `b00bcad…` archive and native helper pair.

| Case | Duration | Actual original exit evidence and implication |
| --- | --- | --- |
| Running native Quit | 1.849352 s | Browser 84522 exited/reaped zero after native `terminate:`; supervisor 84533 exited zero; command 84534 received SIGTERM. Command and supervisor exits were observed more than 14 seconds before the command's earliest alarm. Raw and mapped receipts report cancelled, native root observed/reaped, and final group-KILL EPERM/cleanup uncertainty. Heartbeat stopped and the previously verified preview listener disappeared. |
| Browser-main loss | 1.910528 s | The watchdog deliberately killed its original unreaped browser 84535; original status/reap confirm SIGKILL. Command 84545 received SIGTERM and supervisor 84544 exited **125**, both more than 14 seconds before the alarm. Independent events and stopped heartbeat/listener support bounded fixture cleanup; no receipt or native-reaping result is fabricated after browser death. |
| Supervisor loss | 16.217055 s | An exact-instance audit signal killed supervisor 84557. Command 84558 continued writing after that exit and until its own 15-second alarm, then had an original SIGALRM event. Freedom reported `WORKSPACE_SUPERVISOR_FAILED`, failed terminal state, no native root-exit/reap proof, truncated output and cleanup uncertainty. Browser 84548 subsequently exited/reaped zero after native Quit. **The command's self-expiry is not product cleanup.** |

Every case had nine observed instances including the watchdog and four framework helpers; all original registrations had known exit statuses. There were no browser/framework emergency signals or unconfirmed roles, and the harness never signaled a command. Each controller was explicitly killed/reaped after product proof as harness disposal. The same watchdog served all three cases: 27 per-case observations correspond to 25 distinct observed PIDs. Case durations sum to 19.976935 seconds; first launch intent to last registered controller exit was 19.980925 seconds. These counts are observational, not complete process-birth accounting or aggregate containment.

All three startups reached the exact packaged entry breakpoint and installed the fixed safeStorage-unavailable shim before resume. The actual raw debugger source and possible-breakpoint responses are retained now; each source is 31,721 bytes with SHA-256 `082efede85f589e203492ec5dac9f5791aa53b30dedcaa7202784706d022324b`, and the first breakable location is zero-based line 1, column 24. Actual packaged identity, app readiness, zero-option supervisor resolution, real app-owned composition, live heartbeat and preview positive controls precede the actions. New trace records copy their identity maps at recording time; the earlier idle snapshot limitation remains unchanged.

The [retained 106-record evidence bundle](macos-packaged-native-lifecycle-2026-09-08.bundle.json) is 588,166 bytes, SHA-256 `87fba268ff936d50db32b70f21ceb3f996e9d6cdcc09c81e02d7c20b31b2b3ec`. The main agent verified every member and reviewed actual original events, direct-owner outcomes and raw/mapped receipts. Exact source changes, commands, clocks, markers, checks and declared redactions are included. Runtime credentials, permits, audit tokens and databases are excluded. Historical unknown exits and the previous identical-byte proof timestamp deviation remain recorded; this campaign neither repairs nor repeats them. Missing optional artifact and startup warnings remain visible.

Together with the separately retained idle run, this completes the unsigned, credential-free **packaged original-root lifecycle checkpoint** for this source/artifact. Detached survival remains a separate pending case. Supervisor failure, escaped descendants, aggregate limits, live providers and signed/notarized release qualification remain open; public termination guarantees are unchanged.
